import React from 'react'
import Header from '../components/Header'

//Connect Header to Home Component
const Home = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Home;
